{
  "$id": "https:\/\/supaterm.com\/data\/supaterm-settings.schema.json",
  "$schema": "https:\/\/json-schema.org\/draft\/2020-12\/schema",
  "additionalProperties": false,
  "description": "User-level Supaterm settings.",
  "properties": {
    "$schema": {
      "default": "https:\/\/supaterm.com\/data\/supaterm-settings.schema.json",
      "description": "Optional schema URL for editor completion and validation.",
      "format": "uri",
      "type": "string"
    },
    "analyticsEnabled": {
      "default": true,
      "description": "Allow anonymous telemetry.",
      "type": "boolean"
    },
    "appearanceMode": {
      "default": "system",
      "description": "App appearance mode.",
      "enum": ["system", "light", "dark"],
      "type": "string"
    },
    "crashReportsEnabled": {
      "default": true,
      "description": "Allow crash reports.",
      "type": "boolean"
    },
    "restoreTerminalLayoutEnabled": {
      "default": true,
      "description": "Restore spaces, tabs, and panes on launch.",
      "type": "boolean"
    },
    "systemNotificationsEnabled": {
      "default": false,
      "description": "Deliver desktop notifications for terminal activity.",
      "type": "boolean"
    },
    "updateChannel": {
      "default": "stable",
      "description": "Select stable or tip updates.",
      "enum": ["stable", "tip"],
      "type": "string"
    }
  },
  "title": "supaterm settings.json",
  "type": "object"
}
