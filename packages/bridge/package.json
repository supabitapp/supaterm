{
  "name": "@supaterm/bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "supaterm-bridge": "src/index.ts"
  },
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile supaterm-bridge",
    "typecheck": "tsc --noEmit",
    "typecheck:tsgo": "tsgo --noEmit",
    "lint": "oxlint src/"
  },
  "dependencies": {
    "@supaterm/shared": "workspace:*"
  }
}
