interface:
  display_name: "Start Linear Task Tab"
  short_description: "Open a Linear task in a repo worktree tab"
  default_prompt: "Use $supaterm-start-linear-task-on-a-new-tab to start SUP-34 in a new Supaterm tab using the repo worktree target."
