[tools]
zig = "0.15.2"
tuist = "4.155.4"
swiftlint = "latest"
xcbeautify = "3.1.4"
pnpm = "latest"
"npm:create-dmg" = "latest"

[hooks]
postinstall = '''
set -e
command -v vp >/dev/null 2>&1 || curl -fsSL https://vite.plus | bash
make install-git-hooks
'''
