#!/bin/sh
set -e

mise run --raw validate-pre-commit

if ! git diff --cached --quiet -- apps/supaterm.com; then
  (
    cd apps/supaterm.com
    vp staged
  )
fi

if ! git diff --cached --quiet -- apps/mac; then
  make mac-lint
fi
